// import React from "react";

// type Props = {
//   children: React.ReactNode; // need to fix
// };

// const aux = (props: Props) => {
//   return <;>{props.children}</div>;
// };

//Need to fix

const aux = (props: any) => {
  return props.children;
};

export default aux;
